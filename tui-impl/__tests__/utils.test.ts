import{describe,it,expect}from"vitest";import{textWidth,truncateText,alignText,clamp,rectContains}from"../src/utils";describe("utils",()=>{it("textWidth",()=>{expect(textWidth("hello")).toBe(5);});it("truncateText",()=>{expect(truncateText("hello world",8)).toBe("hello w…");expect(truncateText("short",10)).toBe("short");});it("alignText right",()=>{expect(alignText("x",5,"right")).toMatch(/^\s+x/);});it("alignText center",()=>{expect(alignText("x",5,"center")).toMatch(/\s*x\s*/);});it("clamp",()=>{expect(clamp(-5,0,10)).toBe(0);expect(clamp(15,0,10)).toBe(10);expect(clamp(5,0,10)).toBe(5);});it("rectContains",()=>{expect(rectContains({x:0,y:0,width:10,height:5},{x:5,y:2})).toBe(true);expect(rectContains({x:0,y:0,width:10,height:5},{x:15,y:2})).toBe(false);});});