import{describe,it,expect}from"vitest";import{resolveWidth,DEFAULTS}from"../src/styles";describe("styles",()=>{it("resolveWidth number",()=>{expect(resolveWidth(42,100)).toBe(42);});it("resolveWidth percent",()=>{expect(resolveWidth("50%",100)).toBe(50);});it("resolveWidth undefined",()=>{expect(resolveWidth(undefined,100)).toBe(0);});it("defaults",()=>{expect(DEFAULTS.flexDirection).toBe("row");expect(DEFAULTS.display).toBe("flex");expect(DEFAULTS.flexGrow).toBe(0);});});