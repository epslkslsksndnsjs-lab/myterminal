import{stripAnsi}from"./ansi";export function textWidth(t:string):number{return stripAnsi(t).length;}export function truncateText(t:string,mw:number):string{const p=stripAnsi(t);if(p.length<=mw)return t;return p.slice(0,mw-1)+"…";}export function alignText(t:string,w:number,a:"left"|"center"|"right"="left"):string{const p=stripAnsi(t);if(p.length>=w)return truncateText(t,w);const pad=w-p.length;if(a==="right")return" ".repeat(pad)+t;if(a==="center")return" ".repeat(Math.floor(pad/2))+t+" ".repeat(Math.ceil(pad/2));return t+" ".repeat(pad);}export function clamp(v:number,min:number,max:number):number{return Math.max(min,Math.min(max,v));}export interface Rect{x:number;y:number;width:number;height:number;}export function rectContains(r:Rect,p:{x:number;y:number}):boolean{return p.x>=r.x&&p.x<r.x+r.width&&p.y>=r.y&&p.y<r.y+r.height;}